name: Build and Push Docker Image

on:
  push:
    branches:
      - main  # Se ejecuta cuando hay cambios en la rama main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del repositorio
      uses: actions/checkout@v4

    - name: Iniciar sesión en Docker Hub
      run: echo "${{ secrets.dckr_pat_RopCXglMVobKtO2JgdDALbuUXDw }}" | docker login -u "${{ secrets.luisamagua }}" --password-stdin

    - name: Construir la imagen Docker
      run: docker build -t ${{ secrets.luisamagua }}/analisis_sentimientos:latest .

    - name: Subir la imagen a Docker Hub
      run: docker push ${{ secrets.luisamagua }}/analisis_sentimientos:latest
